name: Validate monthly Dow daily exhaustion profiles
on:
  workflow_dispatch:
    inputs:
      dataset_run_id:
        description: Evening snapshot run ID
        required: true
        type: string
permissions:
  contents: read
concurrency:
  group: stockai-dow-forecast-${{ inputs.dataset_run_id }}
  cancel-in-progress: false
jobs:
  validation:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip
      - name: Restore exact evening snapshot
        uses: actions/cache/restore@v5
        with:
          path: data/stockai.db
          key: stockai-evening-analysis-${{ inputs.dataset_run_id }}
          fail-on-cache-miss: true
      - name: Install dependencies
        run: python -m pip install -r requirements.txt
      - name: Export market data only
        run: python scripts/export_market_research.py --source data/stockai.db --output reports/market-research.db
      - name: Validate forward outcomes
        run: python scripts/run_dow_peak_forecast_validation.py --database reports/market-research.db --output reports/dow-peak-forecast-validation.json
      - uses: actions/upload-artifact@v6
        with:
          name: dow-peak-forecast-validation-${{ inputs.dataset_run_id }}
          path: reports/dow-peak-forecast-validation.json
          if-no-files-found: error
          retention-days: 14
